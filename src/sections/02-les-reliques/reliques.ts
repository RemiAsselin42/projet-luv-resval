// Section "Les Reliques" — à refaire.
// Le code du grimoire interactif a été supprimé en prévision d'une refonte complète.

import type { SectionInitializer } from '../types';

const initThematicObjectsSection: SectionInitializer = (_context) => {
  return {
    update: () => undefined,
    dispose: () => undefined,
  };
};

export default initThematicObjectsSection;
